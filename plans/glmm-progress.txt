# Progress Log: glmm

## PRD Created: 2026-02-02
Feature: Generalized Linear Mixed Models (GLMM) implementation in Rust
Items: 21 tasks created

---

## Background

This implementation adds GLMM support to tidy-ts, targeting clinical/epidemiological use cases
where hierarchical data structures require random effects modeling (e.g., patients nested in
providers nested in clinics).

### Key Design Decisions

1. **Laplace Approximation** - Matches glmmTMB default, provides good balance of speed/accuracy
2. **Log-Cholesky Parameterization** - Unconstrained optimization for variance components
3. **Sparse Z Matrices** - Block-diagonal for nested, COO for crossed random effects
4. **Reuse GLM IRLS** - Existing infrastructure handles fixed effects estimation

### Reusable Components (from existing codebase)

- `glm_fit_core.rs` - IRLS algorithm (800+ lines)
- `qr_decomposition.rs` - Weighted least squares solver (420 lines)
- `family/*` - Gaussian, Binomial, Poisson, Gamma, InverseGaussian
- `gee/types.rs` - ClusterInfo structure
- `gee/correlation/*` - Exchangeable, AR1, Unstructured patterns

### New Components Required

1. **Random Effects Module** (`random_effects.rs`)
   - Z matrix construction (sparse)
   - Random effect likelihood
   - BLUP extraction

2. **Variance Components Module** (`variance_components.rs`)
   - Log-Cholesky parameterization
   - Theta <-> Sigma transformations
   - Jacobian for REML

3. **Laplace Module** (`laplace.rs`)
   - Joint log-likelihood
   - Mode finding (Newton)
   - Hessian computation
   - Marginal likelihood approximation

4. **Fitting Module** (`fitting.rs`)
   - Profile likelihood
   - Outer optimization (BFGS/Newton)
   - Convergence monitoring

5. **Families Extension**
   - Nbinom1, Nbinom2 families
   - Dispersion parameter handling

### Performance Targets

| Scenario | Target |
|----------|--------|
| 5000 obs, 50 groups, 1 RE | < 1 second |
| 10000 obs, 100 groups, 2 RE | < 5 seconds |
| Coefficient accuracy | Within 1e-4 of glmmTMB |
| SE accuracy | Within 1e-3 of glmmTMB |

---

## Implementation Notes

### 2026-02-02: Item 1 Complete - Module Structure and Type Definitions

Created the foundational GLMM module with core type definitions:

**Files Created:**
- `packages/dataframe/rust/stats/regression/glmm/mod.rs` - Module entry point with documentation
- `packages/dataframe/rust/stats/regression/glmm/types.rs` - Core type definitions

**Files Modified:**
- `packages/dataframe/rust/stats/regression/mod.rs` - Added `pub mod glmm;` export

**Key Types Implemented:**

1. **RandomEffect** - Specifies a random effect term
   - `grouping_var`: Variable used for grouping (e.g., "patient")
   - `terms`: Random effect terms (e.g., ["1", "x"] for intercept + slope)
   - `n_groups`, `group_sizes`, `group_ids`, `group_indices`: Group metadata
   - `covariance`: CovarianceType (Independent, Unstructured, CompoundSymmetry)
   - Helper methods: `n_terms()`, `total_coefficients()`, `n_variance_params()`

2. **GlmmControl** - Control parameters for fitting
   - `max_iter_outer`, `max_iter_inner`: Iteration limits
   - `tol_outer`, `tol_inner`: Convergence tolerances
   - `verbose`, `reml`, `optimizer`: Fitting options
   - `start_theta`, `start_beta`: Optional starting values
   - Builder pattern methods: `with_ml()`, `with_max_iter()`, etc.

3. **GlmmResult** - Complete fit result (extends GlmResult)
   - `glm_result`: Reuses all GLM fixed effects infrastructure
   - `random_effects`: Vec<RandomEffect> specifications
   - `variance_components`: Vec<VarianceComponent> estimates
   - `blups`: Vec<RandomEffectEstimates> (BLUPs with optional SE)
   - `theta`, `theta_se`: Variance component parameterization
   - `log_likelihood`, `aic`, `bic`, `reml_criterion`: Fit statistics
   - `converged`, `convergence_message`, `outer_iterations`: Convergence info

4. **VarianceComponent** - Variance estimates for one random effect
   - `vcov`: Variance-covariance matrix
   - `std_dev`: Standard deviations
   - `correlation`: Optional correlation matrix
   - `std_errors`: Optional SE of variance parameters

5. **RandomEffectEstimates** - BLUPs for one grouping factor
   - `estimates`: n_groups x n_terms matrix
   - `std_errors`: Optional conditional SE
   - `conditional_vcov`: Optional full conditional variance

6. **CovarianceType** - Enum for covariance structures
7. **GlmmOptions** - Options for BLUP computation, weights, etc.
8. **GlmmFitSummary** - Summary statistics

**Verification:**
- `cargo check` passes
- `pnpm check` passes (all green)
- Unit tests included in types.rs

**Next Priority:** Item 2 (sparse Z matrix construction) or Item 4 (negative binomial families)
- Item 4 is independent and could unblock family-specific GLMM testing
- Item 2 is the natural next step in the core algorithm

---

### 2026-02-02: Items 2 & 3 Complete - Sparse Z Matrix Construction

Implemented the random_effects module with sparse Z matrix construction for both nested and crossed random effects.

**Files Created:**
- `packages/dataframe/rust/stats/regression/glmm/random_effects.rs` - Full sparse Z matrix implementation

**Files Modified:**
- `packages/dataframe/rust/stats/regression/glmm/mod.rs` - Added `pub mod random_effects;` and re-exports

**Key Components Implemented:**

1. **SparseMatrix** - CSR (Compressed Sparse Row) format
   - `row_ptr`: Row pointers (length nrow+1)
   - `col_idx`: Column indices for non-zeros
   - `values`: Non-zero values
   - Methods: `mul_vec()`, `transpose_mul_vec()`, `weighted_cross_product()`, `to_dense()`

2. **construct_z_matrix()** - Single random effect Z construction
   - Takes RandomEffect spec + term values
   - Returns sparse Z matrix (n × n_groups*n_terms)
   - Handles intercepts, slopes, and multiple terms per group

3. **construct_combined_z_matrix()** - Crossed random effects
   - Concatenates Z blocks for multiple random effects
   - Handles patient × provider crossed designs
   - Column offsets computed automatically

4. **Helper Functions:**
   - `intercept_term_values()` - Creates all-1s vector for intercepts
   - `intercept_slope_term_values()` - Creates [1s, slope_values] for random slopes
   - `populate_random_effect()` - Fills group info from raw string values

**Comprehensive Test Suite:**
- `test_sparse_matrix_basic` - CSR format validation
- `test_sparse_mul_vec` - Z * b multiplication
- `test_sparse_transpose_mul_vec` - Z^T * v multiplication
- `test_construct_z_intercept_only` - 100 obs, 10 groups, random intercept
- `test_construct_z_intercept_slope` - 100 obs, 10 groups, random intercept + slope
- `test_crossed_random_effects` - 100 obs, 10 patients × 5 providers
- `test_weighted_cross_product` - Z^T * W * Z computation
- `test_to_dense` - Sparse to dense conversion
- `test_populate_random_effect` - Group mapping from strings

**Verification:**
- `cargo check` passes
- `pnpm check` passes (all green)
- All unit tests pass (verified via cargo test)

**Design Notes:**
- CSR format chosen for efficient row-based operations (matches IRLS iteration)
- Sparse representation saves memory for large n_groups
- Dense conversion available for small problems
- `weighted_cross_product()` enables efficient Z^T W Z computation for Laplace Hessian

**Next Priority:** Item 5 (variance component parameterization) - log-Cholesky transformation
- This is needed before Item 6 (random effects likelihood) and Item 7 (Laplace approximation)
- Item 4 (negative binomial) is still independent and could be parallelized

---

### 2026-02-02: Item 5 Complete - Variance Component Parameterization (Log-Cholesky)

Implemented the variance_components module with log-Cholesky parameterization for variance components.

**Files Created:**
- `packages/dataframe/rust/stats/regression/glmm/variance_components.rs` - Full implementation (~450 lines)

**Files Modified:**
- `packages/dataframe/rust/stats/regression/glmm/mod.rs` - Added module export and re-exports

**Key Functions Implemented:**

1. **Core Transformations:**
   - `theta_to_cholesky()` - Convert unconstrained theta → Cholesky factor L
   - `cholesky_to_vcov()` - Convert L → covariance matrix Σ = LL^T
   - `theta_to_vcov()` - Combined theta → Σ transformation
   - `vcov_to_theta()` - Inverse transformation Σ → theta (via Cholesky decomposition)
   - `cholesky_decompose()` - Standard Cholesky decomposition of positive-definite matrix

2. **Derived Quantities:**
   - `theta_to_sd()` - Extract standard deviations from theta
   - `theta_to_corr()` - Compute correlation matrix from theta
   - `vcov_to_corr()` - Convert covariance to correlation matrix

3. **Jacobian for REML:**
   - `theta_to_vcov_jacobian()` - Numerical Jacobian of theta → vcov transformation
   - `log_jacobian_determinant()` - Analytical log|J| for likelihood adjustment
   - `log_jacobian_determinant_gradient()` - Gradient of log|J| w.r.t. theta

4. **Initialization Helpers:**
   - `initial_theta()` - Create starting theta for different covariance structures
   - `theta_to_variance_component()` - Convert theta to VarianceComponent struct
   - `total_theta_params()` - Count total parameters across random effects
   - `split_theta()` - Split combined theta vector into per-effect slices

**Parameterization Details:**

For a k×k covariance matrix:
- First k parameters: `theta[0..k] = log(diag(L))` (ensures positive diagonal)
- Remaining k(k-1)/2 parameters: `theta[k..] = L[i,j]` for i > j (off-diagonal, unconstrained)

This gives k(k+1)/2 parameters total, matching lme4/glmmTMB convention.

**Supported Covariance Structures:**
- `Independent`: k params (diagonal only, no correlation)
- `Unstructured`: k(k+1)/2 params (full log-Cholesky)
- `CompoundSymmetry`: 2 params (shared variance, shared correlation via tanh transform)

**Test Coverage (17 tests):**
- `test_theta_to_cholesky_1x1` / `test_theta_to_cholesky_2x2`
- `test_cholesky_to_vcov_1x1` / `test_cholesky_to_vcov_2x2`
- `test_theta_to_vcov_2x2`
- `test_round_trip_1x1` / `test_round_trip_2x2` / `test_round_trip_3x3` - Critical tests!
- `test_theta_to_sd` / `test_theta_to_corr_uncorrelated` / `test_theta_to_corr_correlated`
- `test_cholesky_decompose_2x2` / `test_cholesky_decompose_not_positive_definite`
- `test_initial_theta_*` (independent, unstructured, compound_symmetry)
- `test_theta_to_variance_component_*` (independent, compound_symmetry)
- `test_log_jacobian_determinant` / `test_log_jacobian_determinant_gradient`
- `test_total_theta_params` / `test_split_theta`

**Verification:**
- `cargo check` passes (no errors)
- `pnpm check` passes (all green)

**Numerical Properties:**
- Tolerance: 1e-10 for round-trip tests
- Ensures positive-definite matrices (returns None if not)
- Handles edge cases (k=0, k=1)

**Next Priority:** Item 6 (random effects log-likelihood)
- Requires variance_components for Σ computation
- Implements log p(b | theta) = -0.5 * b' Σ^{-1} b - 0.5 log|Σ|
- Will need gradients d/db and d/dtheta

---

### 2026-02-02: Item 6 Complete - Random Effects Log-Likelihood

Implemented the random_effects_likelihood module for computing log p(b | theta) and its gradients.

**Files Created:**
- `packages/dataframe/rust/stats/regression/glmm/random_effects_likelihood.rs` (~450 lines)

**Files Modified:**
- `packages/dataframe/rust/stats/regression/glmm/mod.rs` - Added module export and re-exports

**Key Functions Implemented:**

1. **Core Likelihood Computation:**
   - `log_random_effects_prior(b, theta, random_effects)` → `RandomEffectsPrior`
     - Computes log p(b | theta) for all random effects across all groups
     - Returns log-likelihood, gradient w.r.t. b, and gradient w.r.t. theta
   - `log_random_effects_prior_value(b, theta, random_effects)` → `f64`
     - Faster version returning only the log-likelihood (no gradients)

2. **Single Group Computation:**
   - `log_prior_one_group(b_group, theta_re, k, cov_type)` → (log_lik, grad_b, grad_theta)
     - Core computation for one group's contribution to the prior
     - Uses Cholesky decomposition for numerical stability

3. **Supporting Functions:**
   - `compute_vcov_for_type(theta_re, k, cov_type)` → covariance matrix
     - Supports Independent, Unstructured, and CompoundSymmetry
   - `forward_solve(L, b)` - Solves L*y = b via forward substitution
   - `backward_solve(L, y)` - Solves L^T*x = y via back substitution
   - `compute_precision_matrix(theta_re, k, cov_type)` → Σ^{-1}
   - `compute_block_precision_matrices(theta, random_effects)` → list of precision matrices

4. **Gradient Computation:**
   - Analytical gradient d/db: `-Σ^{-1} * b`
   - Numerical gradient d/dtheta: Central differences with ε=1e-7

**Mathematical Formula:**
```
log p(b | θ) = -0.5 * b' Σ^{-1} b - 0.5 log|Σ| - (q/2) log(2π)

where:
- b is the random effects vector
- Σ is the covariance matrix derived from θ
- q is the dimension of b
```

**Data Structures:**

```rust
pub struct RandomEffectsPrior {
    pub log_likelihood: f64,    // log p(b | theta)
    pub grad_b: Vec<f64>,       // d/db
    pub grad_theta: Vec<f64>,   // d/dtheta
}
```

**Block Structure:**
- Σ is block-diagonal with one block per group within each random effect
- Each group's contribution computed independently (efficient for large n_groups)
- Memory scales linearly with n_groups, not quadratically

**Test Coverage (15 tests):**
- `test_forward_solve` - Verifies forward substitution
- `test_backward_solve` - Verifies back substitution
- `test_log_prior_one_group_identity` - Identity covariance
- `test_log_prior_one_group_scaled` - Scaled variance (σ² = 4)
- `test_log_prior_two_terms_independent` - Multiple independent terms
- `test_log_prior_full` - Multiple groups
- `test_log_prior_value_only` - Consistency between full and value-only
- `test_precision_matrix_identity` - Precision of identity matrix
- `test_precision_matrix_scaled` - Precision with scaled variance
- `test_precision_matrix_2x2` - 2x2 unstructured covariance
- `test_gradient_b_numerical_check` - Analytical vs numerical gradient d/db
- `test_gradient_theta_numerical_check` - Gradient w.r.t. theta
- `test_crossed_random_effects` - Multiple random effect types

**Verification:**
- `cargo check` passes (no errors)
- `pnpm check` passes (all green)

**Numerical Properties:**
- Tolerance: 1e-8 for equality checks
- Handles non-positive-definite matrices (returns NEG_INFINITY)
- Uses Cholesky decomposition for numerical stability

**Next Priority:** Item 7 (Laplace approximation for marginal likelihood)
- Builds on random_effects_likelihood for the prior term
- Needs joint log-likelihood: log p(y | b, β, θ) + log p(b | θ)
- Newton optimization to find mode b_hat
- Laplace approximation via Hessian

